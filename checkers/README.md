# checkers/

One HTML file per lesson checker. Empty until the kit is ported.

Naming follows the geometry repo: `unit3-day3-diversification.html`,
`unit2-day3-health-insurance.html`. The hub links here with a plain
`hub-card` class (no `hub-ext`) because these live in this repo.
