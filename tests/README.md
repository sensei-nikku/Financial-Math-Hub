# tests/

jsdom tests. `npm install`, then run whatever harness the geometry repo uses.

The money primitives are the ones that actually need tests — currency and
percent parsing have a lot of input shapes and every one of them is a student
who typed something reasonable.
